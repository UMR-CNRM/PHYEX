# mesocentre ESPRI
if [[ "$(hostname)" =~ spiritx[1-2].ipsl.fr || "$(hostname)" =~ spirit[1-2].ipsl.fr ]]; then
  module load gcc/11.2.0
fi

# Météo-France HPC
if [[ "$(hostname)" =~ belenos.*\.meteo.fr || "$(hostname)" =~ taranis.*\.meteo.fr ]]; then
  module load cmake/3.24.1
  module load gcc/14.1.0
fi
